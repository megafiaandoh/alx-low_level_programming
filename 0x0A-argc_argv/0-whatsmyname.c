#include "main.h"

/**
 * main - prints program
 * @argv: array of arguments
 * @argc: parameter to be used
 * Return: Always 0 (success)
 */

int main(int argc, char *argv[])
{
	printf("%s\n", *argv);
	return (0);
}
